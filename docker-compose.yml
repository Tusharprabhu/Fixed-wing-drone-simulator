version: '3.8'

services:
  trainer:
    build: .
    volumes:
      - ./:/workspace
    working_dir: /workspace
    environment:
      - RUN_ID=DroneAgent_DockerRun
    ports:
      - "5004:5004"
    command: ["Assets/DroneAgent.yaml", "--run-id=${RUN_ID}"]
    restart: unless-stopped
